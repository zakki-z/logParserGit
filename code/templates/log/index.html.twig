{% extends 'base.html.twig' %}

{% block title %}Log Entries{% endblock %}

{% block body %}
    <!--header-->
    <header class="flex items-center justify-between border-b border-gray-300 px-10 py-4 bg-white shadow-sm">
        <div>
            <h2 class="text-2xl font-bold text-gray-800">Log Analyzer</h2>
        </div>

        <div class="flex items-center space-x-4">
            {% if app.user %}
                <div class="flex items-center space-x-2">
                    <span class="text-gray-700">
                        <button class="px-4 py-2 bg-gray-200 hover:bg-gray-300 rounded-lg transition duration-200">
                            <a href="#" class="text-gray-800">User Info</a>
                        </button>
                    </span>
                    <form action="{{ path('app_logout') }}" method="post" style="display: inline;">
                        <button type="submit" class="px-4 py-2 bg-red-600 hover:bg-red-700 text-white font-semibold rounded-lg transition duration-200">
                            Logout
                        </button>
                    </form>
                </div>
            {% endif %}
        </div>
    </header>
    <div class="container mx-auto px-4 py-8">
        <div class="bg-white rounded-lg shadow-md p-6">
            <div class="flex items-center justify-between mb-6">
                <h1 class="text-3xl font-bold text-gray-800">Log Entries</h1>
                <a href="{{ path('app_upload') }}" class="text-blue-600 hover:text-blue-800 font-medium">
                    Upload Another File →
                </a>
            </div>
            <!--filter-->
            <!--type filter-->
            <div class="mb-4">
                <label for="severityFilter" class="block text-sm font-medium text-gray-700 mb-1">Filter by Type:</label>
                <form action="{{ path('app_log_view') }}" method="get" class="inline">
                    <select id="severityFilter" name="severity" class="form-select rounded-md border-gray-300 text-sm" onchange="this.form.submit()">
                        <option value="">All Types</option>
                        <option value="ERROR" {% if severityFilter == 'ERROR' %}selected{% endif %}>ERROR</option>
                        <option value="WARNING" {% if severityFilter == 'WARNING' %}selected{% endif %}>WARNING</option>
                        <option value="INFO" {% if severityFilter == 'INFO' %}selected{% endif %}>INFO</option>
                        <option value="DEBUG" {% if severityFilter == 'DEBUG' %}selected{% endif %}>DEBUG</option>
                    </select>
                    {% if pager.hasNextPage or pager.hasPreviousPage %}
                        <input type="hidden" name="page" value="{{ pager.currentPage }}">
                    {% endif %}
                </form>
            </div>
            <!--time filter-->
            {% set dateFrom = dateFrom|default(app.request.query.get('dateFrom')) %}
            {% set dateTo = dateTo|default(app.request.query.get('dateTo')) %}
            <label for="dateFromFilter" class="block text-sm font-medium text-gray-700 mb-1">Filter by Date:</label>
            <span class="text-gray-500">from</span>
            <input type="hidden" id="dateFrom" name="dateFrom" value="{{ dateFrom|date('Y-m-d') }}">
            <input type="hidden" id="dateTo" name="dateTo" value="{{ dateTo|date('Y-m-d') }}">
            <input type="date" id="dateFromFilter" name="dateFromFilter" class="form-input rounded-md border-gray-300 text-sm" value="{{ dateFrom|date('Y-m-d') }}" onchange="this.form.submit()">
            <span class="text-gray-500">to</span>
            <input type="date" id="dateToFilter" name="dateToFilter" class="form-input rounded-md border-gray-300 text-sm" value="{{ dateTo|date('Y-m-d') }}" onchange="this.form.submit()">
            <!--buttons-->
            <button class="px-3 py-2 text-sm font-medium text-gray-700 bg-gray-100 hover:bg-gray-200 rounded-md">
                Reset Filters
            </button>
            <div class="flex items-center justify-end  mb-4 gap-[2vw]">
                <p class="text-gray-600">Total entries: <strong class="text-gray-800">{{ pager.nbResults }}</strong></p>
                <form action="{{ path('log_clear') }}" method="post" style="display: inline;">
                    <button type="submit" class="px-4 py-2 text-sm font-medium text-red-700 bg-red-100 hover:bg-red-200 rounded-lg transition duration-200">
                        Clear Everything
                    </button>
                </form>
                <form action="{{ path('log_pdf') }}" method="get" style="display: inline;">
                    <button type="submit" class="px-4 py-2 text-sm font-medium text-green-700 bg-green-100 hover:bg-green-200 rounded-lg transition duration-200">
                        Download PDF
                    </button>
                </form>
            </div>

            <div class="table-responsive overflow-x-auto">
                <table class="table table-striped table-hover w-full border border-gray-200">
                    <thead class="bg-gray-800 text-white">
                    <tr>
                        <th class="px-4 py-3 text-left">ID</th>
                        <th class="px-4 py-3 text-left">Date</th>
                        <th class="px-4 py-3 text-left">Channel</th>
                        <th class="px-4 py-3 text-left">Type</th>
                        <th class="px-4 py-3 text-left">File</th>
                        <th class="px-4 py-3 text-left">Information</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for entry in logEntries %}
                        <tr class="border-b border-gray-200 hover:bg-gray-50">
                            <td class="px-4 py-3">{{ entry.id }}</td>
                            <td class="px-4 py-3">{{ entry.date|date('Y-m-d H:i:s') }}</td>
                            <td class="px-4 py-3">
                                <span class="badge bg-secondary px-3 py-1 rounded-full text-sm bg-gray-200 text-gray-800">{{ entry.channel }}</span>
                            </td>
                            <td class="px-4 py-3">
                                {% if entry.type == 'ERROR' %}
                                    <span class="badge bg-danger px-3 py-1 rounded-full text-sm bg-red-600 text-white">{{ entry.type }}</span>
                                {% elseif entry.type == 'WARNING' %}
                                    <span class="badge bg-warning px-3 py-1 rounded-full text-sm bg-yellow-500 text-gray-900">{{ entry.type }}</span>
                                {% elseif entry.type == 'INFO' %}
                                    <span class="badge bg-info px-3 py-1 rounded-full text-sm bg-blue-500 text-white">{{ entry.type }}</span>
                                {% elseif entry.type == 'DEBUG' %}
                                    <span class="badge bg-primary px-3 py-1 rounded-full text-sm bg-indigo-600 text-white">{{ entry.type }}</span>
                                {% else %}
                                    <span class="badge bg-secondary px-3 py-1 rounded-full text-sm bg-gray-200 text-gray-800">{{ entry.type }}</span>
                                {% endif %}
                            </td>
                            <td class="px-4 py-3">{{ entry.file.fileName }}</td>
                            <td class="px-4 py-3">{{ entry.information }}</td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="5" class="px-4 py-8 text-center text-gray-500">
                                No log entries were found.
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
            <!--pager-->
            <div class="flex justify-center mt-6">
                <nav class="flex space-x-2">
                    {% if pager.hasPreviousPage %}
                        <a href="{{ path('app_log_view', {'page': pager.previousPage}) }}"
                           class="px-4 py-2 text-sm font-medium text-gray-700 bg-white border border-gray-300 rounded-md hover:bg-gray-50">
                            « Previous
                        </a>
                    {% endif %}
                    <span class="px-4 py-2 text-sm font-medium text-gray-700">
                        Page {{ pager.currentPage }} of {{ pager.nbPages }}
                    </span>

                    {% if pager.hasNextPage %}
                        <a href="{{ path('app_log_view', {'page': pager.nextPage}) }}"
                           class="px-4 py-2 text-sm font-medium text-gray-700 bg-white border border-gray-300 rounded-md hover:bg-gray-50">
                            Next »
                        </a>
                    {% endif %}
                </nav>
            </div>
        </div>
    </div>
{% endblock %}
